---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - kafka_service_broker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: kafka-service-broker
    version: latest
