set -e -x

mkdir -p ${BOSH_INSTALL_TARGET}/target/

cp -a kafka-service-broker/kafka-broker/target/kafka-broker.jar ${BOSH_INSTALL_TARGET}/target/
cp -a kafka-service-broker/kafka-broker/manifest.yml ${BOSH_INSTALL_TARGET}/
